import { PureExamplePipe } from './pure-example.pipe';

describe('PureExamplePipe', () => {
  it('create an instance', () => {
    const pipe = new PureExamplePipe();
    expect(pipe).toBeTruthy();
  });
});
